server:
  shutdown: graceful

spring:
  application:
    name: spring-kafka-21-simple

  kafka:
    consumer:
      group-id: default-spring-consumer3
      auto-offset-reset: earliest

management:
  endpoint:
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include: shutdown