02. Навигация по системе

1) . - текущая директория
2) .. - директория выше
если мы пытаемся попасть в директорию, которой выше уже нет, то мы остаемся в текущей
cd ..

3)pwd - текущая директория

4) ls -la  - обе команды показывают еще и скрытые файлы
ls -a
ls -la -R /